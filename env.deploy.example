# Deploy-time environment variables
# Used by scripts/azure_deploy_container.py - NOT uploaded to Key Vault

# Azure authentication (set via 'az login' or GitHub Actions OIDC)
AZURE_SUBSCRIPTION_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_ID=

# Resource configuration
AZURE_RESOURCE_GROUP=protected-azure-container-rg
AZURE_LOCATION=westeurope

# Container image
CONTAINER_IMAGE=ghcr.io/your-user/protected-azure-container:latest

# Domain and TLS
PUBLIC_DOMAIN=your-domain.com
ACME_EMAIL=you@your-domain.com



# Registry credentials (for private GHCR images)
GHCR_USERNAME=

GHCR_TOKEN=

# Basic Auth (required for deployment)
BASIC_AUTH_USER=admin
BASIC_AUTH_HASH=
